pipeline {
    agent any

    stages {
        stage('Preparação') {
            steps {
                script {
                    echo "Verificando conteúdo do diretório..."
                    bat "dir" 
                    echo "Preparando o ambiente Docker..."
                    bat "docker build -f testes.dockerfile -t coberturateste ."
                }
            }
        }

        stage('Rodar Testes') {
            steps {
                script {
                    echo "Executando os testes dentro do contêiner Docker..."
                    bat "docker run --rm -v %cd%/test-results:/app/test-results coberturateste poetry run pytest --junitxml=/app/test-results/results.xml"
                }
            }
        }
    }
}
